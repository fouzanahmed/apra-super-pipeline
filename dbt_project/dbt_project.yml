name: super_pipeline
version: "1.0.0"
config-version: 2

profile: super_pipeline

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  super_pipeline:
    staging:
      +schema: staging
      +materialized: view
    mart:
      +schema: mart
      +materialized: table
